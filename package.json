{
  "name": "streamed-addon",
  "version": "1.0.3",
  "description": "Stremio addon for live sports from the Streamed (streamed.pk) API — catalogs, metadata and sorted streams.",
  "main": "src/app/server.js",
  "scripts": {
    "start": "node src/app/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
